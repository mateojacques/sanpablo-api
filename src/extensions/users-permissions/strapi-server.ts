export default (plugin: any) => {
  // Aquí podemos extender controladores / servicios si se necesita más tarde
  // Ejemplo: plugin.controllers.user.find = async (ctx) => { ... }
  return plugin;
};
